<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Personal Portfolio</title>
    <style>
        :root {
            --primary-color: #333;
            --secondary-color: #007bff;
            --background-light: #f4f4f4;
            --text-light: white;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: 'Arial', sans-serif;
            line-height: 1.6;
            color: var(--primary-color);
        }

        #navbar {
            position: fixed;
            top: 0;
            width: 100%;
            background-color: var(--primary-color);
            color: var(--text-light);
            text-align: center;
            padding: 15px 0;
            z-index: 1000;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }

        #navbar ul {
            list-style: none;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        #navbar ul li {
            margin: 0 15px;
        }

        #navbar ul li a {
            color: var(--text-light);
            text-decoration: none;
            font-weight: bold;
            transition: color 0.3s ease;
        }

        #navbar ul li a:hover {
            color: var(--secondary-color);
        }

        #welcome-section {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: linear-gradient(rgba(0,0,0,0.6), rgba(0,0,0,0.6)), 
                        url('https://via.placeholder.com/1500') no-repeat center center/cover;
            color: var(--text-light);
            text-align: center;
            padding: 0 20px;
        }

        #welcome-section h1 {
            font-size: 3.5em;
            margin-bottom: 20px;
        }

        #welcome-section p {
            font-size: 1.2em;
            max-width: 600px;
        }

        #projects {
            padding: 50px 20px;
            background-color: var(--background-light);
            text-align: center;
        }

        #projects h2 {
            font-size: 2.5em;
            margin-bottom: 40px;
        }

        .projects-container {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            gap: 20px;
        }

        .project-tile {
            width: 300px;
            background-color: white;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
            padding: 20px;
            transition: transform 0.3s ease;
        }

        .project-tile:hover {
            transform: scale(1.05);
        }

        .project-tile a {
            text-decoration: none;
            color: var(--primary-color);
            font-weight: bold;
            display: block;
        }

        .project-tile p {
            margin-top: 10px;
            color: #666;
        }

        #contact {
            padding: 50px 20px;
            text-align: center;
            background-color: var(--primary-color);
            color: var(--text-light);
        }

        #contact h2 {
            font-size: 2.5em;
            margin-bottom: 20px;
        }

        #profile-link {
            color: var(--secondary-color);
            text-decoration: none;
            font-weight: bold;
            display: inline-block;
            margin-top: 20px;
            transition: color 0.3s ease;
        }

        #profile-link:hover {
            color: #0056b3;
        }

        @media (max-width: 768px) {
            #navbar ul {
                flex-direction: column;
            }

            #navbar ul li {
                margin: 10px 0;
            }

            #welcome-section h1 {
                font-size: 2.5em;
            }

            #welcome-section p {
                font-size: 1em;
            }

            .projects-container {
                flex-direction: column;
                align-items: center;
            }

            .project-tile {
                width: 90%;
                max-width: 400px;
            }
        }

        @media (max-width: 480px) {
            #welcome-section h1 {
                font-size: 2em;
            }

            #projects h2, #contact h2 {
                font-size: 2em;
            }
        }
    </style>
</head>
<body>
    <!-- Navbar -->
    <nav id="navbar">
        <ul>
            <li><a href="#welcome-section">Welcome</a></li>
            <li><a href="#projects">Projects</a></li>
            <li><a href="#contact">Contact</a></li>
        </ul>
    </nav>

    <!-- Welcome Section -->
    <section id="welcome-section">
        <h1>Welcome to My Portfolio</h1>
        <p>Hi, I'm [Your Name], a web developer passionate about building creative and impactful solutions. With expertise in front-end technologies, I transform ideas into elegant, user-friendly web experiences.</p>
    </section>

    <!-- Projects Section -->
    <section id="projects">
        <h2>My Projects</h2>
        <div class="projects-container">
            <div class="project-tile">
                <a href="https://github.com/yourusername/project1" target="_blank">Project 1: Web App</a>
                <p>A responsive web application demonstrating modern front-end design and functionality.</p>
            </div>
            <div class="project-tile">
                <a href="https://github.com/yourusername/project2" target="_blank">Project 2: Interactive Dashboard</a>
                <p>An interactive data visualization dashboard with real-time updates and smooth animations.</p>
            </div>
        </div>
    </section>

    <!-- Contact Section -->
    <section id="contact">
        <h2>Contact Me</h2>
        <p>Interested in collaboration or have a project in mind? Let's connect!</p>
        <a id="profile-link" href="https://github.com/yourusername" target="_blank">Visit my GitHub Profile</a>
    </section>
</body>
</html>
